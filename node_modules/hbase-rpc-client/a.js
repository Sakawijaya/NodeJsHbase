h = require('./')

var hbase = h
var client = hbase({
    zookeeperHosts: 'localhost:2181',
    zookeeperRoot: '/hbase',
    rootRegionZKPath: '/root-region-server',
    rpcTimeout: 30000,
    pingTimeout: 30000,
    callTimeout: 5000
});

client.on('error', function(err) {
  console.log(err)
})

var del;
del = new hbase.Delete("asdsd");
client.delete("tablename" ,del, function(err, res) {
    return console.log(arguments);
});
